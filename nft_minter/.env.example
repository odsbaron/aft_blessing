# ========== 网络配置 ==========
# amoy - Polygon Amoy 测试网 (推荐用于开发测试)
# polygon - Polygon 主网 (正式环境)
NETWORK=amoy

# ========== 合约配置 ==========
# 部署合约后，将合约地址填入此处
# 示例: CONTRACT_ADDRESS=0x1234567890123456789012345678901234567890
CONTRACT_ADDRESS=

# ========== RPC 节点配置 ==========
# 默认使用公共节点，生产环境建议使用 Infura 或 Alchemy
# 测试网 RPC (自动选择，仅参考)
RPC_URL_AMOY=https://rpc-amoy.polygon.technology
# 主网 RPC (自动选择，仅参考)
RPC_URL_POLYGON=https://polygon-rpc.com

# ========== 钱包私钥 ==========
# 警告：此私钥用于后端签名部署合约
# 生产环境请使用专用部署钱包，勿使用存有大资金的钱包！
# 获取方式：MetaMask -> 账户详情 -> 导出私钥
# 格式：64位十六进制字符串，不带 0x 前缀
PRIVATE_KEY=

# ========== IPFS 配置 (Pinata) ==========
# 推荐使用 JWT 认证，更安全
# 获取方式：登录 Pinata → API Keys → Create New Key
PINATA_JWT=

# 或使用 API Key + Secret（旧方式，二选一）
PINATA_API_KEY=
PINATA_API_SECRET=

# IPFS 网关 (默认使用 Pinata)
IPFS_GATEWAY=https://gateway.pinata.cloud/ipfs

# ========== NFT 元数据配置 ==========
NFT_NAME=Birthday Memorial
NFT_SYMBOL=BDAY
# 基础URI指向IPFS上的图片文件夹
# 上传图片到IPFS后，将文件夹CID替换下面的示例CID
NFT_BASE_URI=ipfs://QmXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX/

# ========== Gas 配置 ==========
GAS_LIMIT=300000      # Gas限制（每个NFT）
GAS_PRICE_GWEI=30    # Gas价格（Gwei）

# ========== Flask 应用配置 ==========
SECRET_KEY=nft-minter-secret-key-change-this

# ========== 高级配置（可选） ==========
# 自定义 RPC 节点（推荐用于生产环境）
# CUSTOM_RPC_URL=

# 区块浏览器（用于查看交易）
# EXPLORER_URL_AMOY=https://amoy.polygonscan.com
# EXPLORER_URL_POLYGON=https://polygonscan.com
